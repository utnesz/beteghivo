import { VizsgalatController } from './vizsgalat-controller';

describe('VizsgalatController', () => {
  it('should create an instance', () => {
    expect(new VizsgalatController()).toBeTruthy();
  });
});
